/* =====================================================
   main.js — Portfolio Home Page Scripts
   ===================================================== */
 
// Currently no custom JavaScript is needed for the home page.
// Bootstrap's bundle (loaded via CDN in index.html) handles
// any interactive Bootstrap components (dropdowns, collapse, etc.).
 
// ── Placeholder: Smooth scroll for the scroll indicator ──
// TODO: Uncomment and adjust the target selector once the
//       "below the fold" section is added to index.html.
//
// document.querySelector('.scroll-indicator')?.addEventListener('click', () => {
//   document.querySelector('#about')?.scrollIntoView({ behavior: 'smooth' });
// });
 
 
// ── Placeholder: Active nav highlighting ─────────────────
// TODO: If this site becomes a true single-page app (SPA)
//       rather than separate HTML files, swap static .active
//       classes for dynamic detection based on scroll position
//       or URL hash.